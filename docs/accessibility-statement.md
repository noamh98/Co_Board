<!--
  Accessibility Statement — Co_Board
  הצהרת נגישות לפי תקן ישראלי ת"י 5568 (המאמץ את WCAG 2.0 רמה AA).

  ⚠️ טיוטה טכנית — הסעיפים המשפטיים המחייבים מסומנים [TBD — counsel]
  וחייבים אישור/ניסוח יועץ משפטי לפני פרסום ציבורי.
-->

# הצהרת נגישות — Co_Board

**רמת התאמה:** התאמה חלקית (Partially Conformant) לתקן הישראלי **ת"י 5568**,
המאמץ את הנחיות **WCAG 2.0 רמה AA**.

**תאריך עדכון אחרון:** 2025-01 (v1)
**גרסת מסמך:** 1.0

> ⚠️ **הערת עריכה (אינה חלק מההצהרה הפומבית):** מסמך זה הוא טיוטה טכנית.
> כל סעיף המסומן `[TBD — counsel]` — לרבות השם המשפטי של הגוף, זהות ופרטי
> רכז/ת הנגישות, ונוסח סטטוטורי מדויק — טעון השלמה ואישור של יועץ משפטי
> לפני פרסום ציבורי.

---

## 1. מחויבות לנגישות

‏Co_Board היא אפליקציית תקשורת תומכת וחלופית (AAC) עברית-first, המיועדת
לילדים ולמבוגרים עם מוגבלות דיבור. הנגשת המוצר לכלל המשתמשים, ובכללם
אנשים עם מוגבלות, היא ערך ליבה של המוצר ולא דרישת רגולציה בלבד.

אנו פועלים להתאים את האפליקציה להוראות תקנות שוויון זכויות לאנשים עם מוגבלות
(התאמות נגישות לשירות), התשע"ג-2013, ולתקן הישראלי ת"י 5568.

## 2. היקף ההצהרה

הצהרה זו חלה על אפליקציית הרשת (PWA) של Co_Board, על כלל המסכים והזרימות
העיקריות: לוח התקשורת, שורת המשפט וההקראה, פאנל ההגדרות, ההצטרפות (onboarding)
וניהול המנוי.

## 3. רמת ההתאמה

האפליקציה **עומדת באופן חלקי** בדרישות ת"י 5568 (WCAG 2.0 AA).
מרבית קריטריוני ההצלחה מתקיימים, לרבות:

- מבנה ARIA תקין ללוח (grid → row → gridcell) עם ניווט מקלדת.
- יעדי מגע גדולים (≥ 44px, ניתן להגדלה).
- תמיכה בסריקת מתגים (switch scanning) עם השהיית-רעד (debounce).
- גופן קריא ידידותי-דיסלקציה ומצב רגיעה חושית.
- הקראה קולית (TTS) עברית, כולל אישור לפני ניקוי וריטוט (haptics) אופציונליים.
- **ניגודיות צבע (WCAG 1.4.3) של 4.5:1 לכל הפחות בכל ערכות הצבע** (בהיר, כהה,
  ניגודיות גבוהה ורגיעה חושית), הנאכפת אוטומטית בבדיקות הנגישות (axe) ללא ויתורים.
- עבודה offline-first; אין בקשות רשת לצד-שלישי לפני הסכמה.

## 4. מגבלות ידועות

נכון למועד עדכון מסמך זה, **קריטריון הניגודיות (WCAG 1.4.3) נסגר**: צבע-המותג
כויל ל-4.5:1 וכלל `color-contrast` נאכף גלובלית בחבילת בדיקות הנגישות
(‏`app/e2e/settings-a11y.spec.ts`) **ללא ויתורים (waivers)**.

עם זאת, ההצהרה מנוסחת כ**"התאמה חלקית"** ולא "התאמה מלאה" מהסיבות הבאות:

- **אישור פורמלי/סטטוטורי:** הנוסח המשפטי המחייב וזהות רכז/ת הנגישות טרם
  הושלמו ואושרו — `[TBD — counsel]`.
- **בדיקה בטכנולוגיות מסייעות:** מטריצת בדיקה מלאה בקוראי-מסך ובמתגי-סריקה
  על מכשירים פיזיים עדיין בתהליך, וטרם תועדה כהתאמה מלאה.

עם השלמת שני אלה תעודכן ההצהרה בהתאם.

## 5. משוב ודרכי פנייה

נשמח לקבל משוב על נגישות האפליקציה. אם נתקלתם ברכיב שאינו נגיש, אנא פנו אלינו:

- **רכז/ת נגישות:** `[TBD — counsel]`
- **דוא"ל:** `[TBD — counsel]`
- **טלפון:** `[TBD — counsel]`
- **כתובת למשלוח דואר:** `[TBD — counsel]`

נשתדל להשיב לפניות בתוך זמן סביר ולטפל בליקויי הנגישות שיובאו לידיעתנו.

## 6. פרטי הגוף וההסדרה המשפטית

- **שם משפטי של הגוף המפעיל:** `[TBD — counsel]`
- **נוסח סטטוטורי מחייב לפי תקנות שוויון זכויות לאנשים עם מוגבלות:** `[TBD — counsel]`

## 7. מועד ההצהרה

- **תאריך פרסום ראשוני:** `[TBD — counsel]`
- **תאריך עדכון אחרון:** 2025-01

---

_מסמך זה מקושר מתוך פאנל ההגדרות באפליקציה. ראו רכיב
`app/src/presentation/settings/AccessibilityStatement.tsx`._
